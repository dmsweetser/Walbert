I want a walbert block specifically for the console response to the user.
It should be in the system prompt, documentation, and be parsed in the response.

Also, I think we have an issue with the prompt.
I want to take advantage of prompt caching, and for that to work, the prompt needs to only be appended as it progresses until the conversation is complete.
At that point, it should reset, but not before.
I think right now, the prompt is being rebuilt instead of appended too often.