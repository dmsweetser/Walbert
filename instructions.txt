Instead of logging all prompts to one file and all responses to one file, I want to log each one individually, with the ticks as the start of the file name, under the session so that you see in chronological order each prompt and response in its own file.