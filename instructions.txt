Can you simplify the state management logic? What I want is for it to maintain in memory the system prompt, db schema, recent conversations, and awareness data. Whenever it is updated, persist the latest to JSON. Load from JSON if present on start.