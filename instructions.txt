Can you fix my test issues in test.results? I think the agent needs to not wait for user input only when testing.

***

The tests get to here and then seem to hang:

```

>>>>> [walbert_console_response] Context validation complete. Ready for next command. [/walbert_console_response] [walbert_summary] Validated context and execution history [/walbert_summary] 

Walbert:
Context validation complete. Ready for next command.



>>>>> 
```

Like they are waiting for something. The goal of the tests was to see that in a simulated response of SQL success or error, or Python success or error, with dialogue history, that the subsequent prompt contained the proper context. I don't think the tests are doing that. Can you revise them to accomplish the original goal?