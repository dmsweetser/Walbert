Can you make it so that when the user enters input, the existing llama-completion is interrupted and a new prompt including the user input is immediately provided to the model for completion? I don't want the user to have to wait for in-progress work. Only interrupt llama-completion, not block execution.

Also, I don't seem to get the console responses back to the console when I have log off. Can you fix that?