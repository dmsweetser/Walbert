When the user adds a message, please immediately add the block to the context block. That way, if they send a second consecutive message, the first one isn't lost.

I tried running pip_install serial, and I got an error "SQLite objects created in a thread can only be used in that same thread". Can you make the SQLite access thread-safe?