Make it so Walbert can process multiple sql or python blocks in a round. Right now, it can only do one of each.

Also, get rid of the user timeout to enter autonomous mode. Instead. give a keyword like quit, call it "auto", to enter autonomous mode. Make it possible for the user to enter any input to return from autonomous mode after it is entered.

When Walbert loads, show the list of keywords, and an ascii moose.