I'm not seeing any data persisted to the database for items. I'd like to change the approach to help with this.

Have the current DB schema be part of the prompt - extract it from the database and provide it to Walbert.

And instead of giving granular, structured access to the DB, allow Walbert to execute any arbitrary SQLITE-compatible SQL statement.

Replace the existing functionality with this new approach.