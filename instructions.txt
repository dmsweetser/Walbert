Get rid of the idea of returning user control.

Instead, the user can interrupt at any time.

Right now, when you do that, it doesn't immediately restart the model server. I need it to.

Fix this and any other issues you see.

For the dialogue history, I want to store x number of entries. An entry will either be a user input or the walbert summary block from the agent.

Also, reset the python and sql results after the round where they are shown.

Please make these changes in as few modifications as possible.