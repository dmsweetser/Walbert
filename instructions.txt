The SQL needs some work. I want you to base64 encode anything you persist to the DB, and base64 decode it on the way out. Get rid of all the syntax / scrubbing behaviors - you won't need to worry about that anymore.

For skills, I want to add some more structure. A skill should have an input object that is the state, it can do whatever internally, and then it should return the same state object back out. The contents of the state object will differ between skills, but it should be a Python object with key value pairs.

Revise the app throughout to align with these requirements.