Please stop including the system prompt as a context block. Instead, just use the in-memory system prompt constructed on demand.

Also, the awareness is never updated in the state JSON - and it isn't ever updated in the generated prompt for the model.

Please fix these - thanks!