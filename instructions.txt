I want Walbert to have full autonomy over its DB and all data persistence. Right now, the schema implies message/conversation persistence, and I have special handling for skills and such that persist and retrieve from the DB in hard-coded ways.

This won't work if Walbert changes its DB schema, and I want it to be able to do that.

Get rid of all hard-coded SQL and references to it - let's keep just the basic items table, and let Walbert figure out the rest.

For example, let it persist and retrieve skills in its own way, rather than through hard-coded means.

Let it persist and retrieve conversation history in its own way, rather than through hard-coded means.

Get rid of any conversation handling code and skill handling code in the app. Only log the IO input/output and raw LLM output to file as the conversation history.

Can you make these changes for me?