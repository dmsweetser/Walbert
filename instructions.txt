I want you to change what is output to the console. By default, have only the console response go to the console. But give a new option in the menu to see the raw log, which will feed all the model output a few lines at a time, allowing the user to page forward or backward or exit the log. That way, the console stays uncluttered, just showing model responses to the user, but you can access all the rest.

Also allow menu options to see just the awareness, db schema, or context blocks.

Also, I need you to allow backspace for the user console input. Right now that isn't possible and it is very annoying.