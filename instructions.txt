I had an epiphany.

Get rid of all the code that gives the DB schema to Walbert.

Instead, make everything more rigid, using walbert blocks for the components (for example, persisting an item).

That way, you can base-64 encode and decode the contents more easily.

So for any part that you need to persist, it should have its own specialized walbert block with its own contents.

A skill may have multiple types of walbert blocks required for persistence. The granularity is important.

Design things so that every walbert block has only one thing in it. The LLM should be entirely ignorant of the base-64 encoding. Just use that in the background so there are no SQL persistence errors.

But for that to work, you need to have the proper granularity for data persistence, so each thing is in its own block.

Make changes everywhere to accommodate this change in approach. Clean up any unused code.

Generate full drop-in replacements for any affected files.
