Get rid of the current implementation for turning internet on or off. Instead, have the python execution process externally block any IP access.

Also, have the console clear when starting, when a user provides a request, and when a console response comes from the agent. I want it to be a clean interface.